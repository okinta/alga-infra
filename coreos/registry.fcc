storage:
  files:
    - path: /etc/containers/auth.json
      mode: 0640
      contents:
        inline: |
          {
            "auths": {
              "$CONTAINER_REGISTRY": {
                "auth": "$CONTAINER_REGISTRY_AUTH"
              }
            }
          }

    - path: /run/containers/auth
      mode: 0644
      contents:
        inline: |
          REGISTRY_AUTH_FILE=/etc/containers/auth.json

networkd:
  units:
    - name: static.network
      contents: |
        [Match]
        Name=eth1

        [Link]
        MTUBytes=1450

        [Network]
        Address=$PRIVATE_IP/24

passwd:
  users:
    - name: regan
      ssh_authorized_keys:
        - "$SSH_KEY"
